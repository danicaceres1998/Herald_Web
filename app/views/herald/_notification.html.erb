<% if @billers.empty? %>
  <h2 class="text-center mt-4 font-italic">The list is empty!</h2>
  <div class="row justify-content-center mt-2">
    <div class="img-fluid mb-5 text-center"></div>
  </div>
<% end %>